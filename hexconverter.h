#ifndef HEXCONVERTER_H
#define HEXCONVERTER_H

#include<QString>

class HexConverter
{
public:
    static QString ToHex(const QString& input);
    static QString ToString(const QString& input);
};

#endif // HEXCONVERTER_H
